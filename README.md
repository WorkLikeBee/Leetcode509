# Leetcode509
Fibonacci  Number

The logic of fibonacci sequence is, F_0 =0, F_1 =1, F_2=1, F_3=2, ... so that F_n = F_(n-1)+F_(n-2). 
To achieve the fib num, I create a solution using recursion 
First I create a base case when n<=1 return n 
Else, return the fib(n-1) + fib(n-2) (recursive case)

